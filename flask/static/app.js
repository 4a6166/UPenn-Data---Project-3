// Flask should pass the var 'query_results', which will be an array of dictionaries of queries to get data needed for visualizations
// example: query_result = [
//   { map_data: ["results from queries needed for map viz"] },
//   { scatter_data: ["results from queries needed for scatter"] },
//   { radar_data: ["results from queries needed for radar viz"] },
//   { slope_data: ["results from queries needed for slope viz"] },
// ]

// TEST SCRIPT
let header = document.getElementsByTagName("h1")

header[0].style.background = 'blue'

